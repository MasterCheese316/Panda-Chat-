{ name: panda-chat, version: 1.0.0, main: server.js, dependencies: { express: ^4.17.1 }, scripts: { start: node server.js } } Now server.js — copy and paste this whole block: const express = require('express'); const app = express(); const PORT = process.env.PORT || 8080; app.use(express.static('public')); const server = app.listen(PORT, () => console.log(Listening on ${PORT})); const io = require('socket.io')(server); io.on('connection', socket => { socket.on('transcript', data => socket.broadcast.emit('transcript', data)); });
